clear all; close all;
im = imread('101_i110.ppm');

i0 = 1; j0 = 1;
[i1 j1 c] = size(im);

maxI = zeros(i1,j1);
minI = zeros(i1,j1); 
for i=i0:i1
    for j=j0:j1
        
        M = max(im(i,j,:));
        m = min(im(i,j,:));  
        b(i,j) = 
               
    end
end

 b = (maxI-minI)./maxI;